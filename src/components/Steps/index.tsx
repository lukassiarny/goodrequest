import React from "react";
import styled from "styled-components";

type Props = {
  currentStep?: number;
};

const Steps: React.FC<Props> = ({ currentStep = 1 }) => {
  return <>Steps</>;
};

export default Steps;
