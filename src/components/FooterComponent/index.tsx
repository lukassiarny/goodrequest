import React from "react";
import styled from "styled-components";
import { FlexContainer } from "../../theme/layout";

const Footer: React.FC = () => {
  return <FlexContainer>Footer</FlexContainer>;
};

export default Footer;
